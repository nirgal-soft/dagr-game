# Local DAGR PostgreSQL database
DATABASE_URL=postgresql://dagr:dagr@localhost:5432/dagr_dev
WORLD_SEED=0

# Compile SQLx query macros from committed metadata; runtime still uses DATABASE_URL.
SQLX_OFFLINE=true
